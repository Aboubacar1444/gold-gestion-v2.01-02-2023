{% extends 'base.html.twig' %}

{% block body %}
    <div class="container justify-content-center text-bold text-center mt-5">
        <h1>Agence</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ agency.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ agency.name }}</td>
                </tr>
                <tr>
                    <th>Tel</th>
                    <td>{{ agency.tel }}</td>
                </tr>
                <tr>
                    <th>Caisse</th>
                    <td>{{ agency.caisse }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('agency_index') }}" class="font-weight-bold">Retour à la liste</a>
        <a class="btn btn-outline-red btn-sm btn-rounded" href="{{ path('agency_edit', {'id': agency.id}) }}">Modif</a>
        {{ include('agency/_delete_form.html.twig') }}
    </div>
    {#  #}
{% endblock %}
