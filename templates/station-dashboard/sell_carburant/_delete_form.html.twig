<form method="post" action="{{ path('app_sell_carburant_delete', {'id': sell_carburant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sell_carburant.id) }}">
    <button class="btn">Supprimer</button>
</form>
