{% extends 'base-station.html.twig' %}


{% block body %}
    <h1 class="text-uppercase text-center">Detail Achat carburant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ buy_carburant.id }}</td>
            </tr>
            <tr>
                <th>Litre</th>
                <td>{{ buy_carburant.litre }}</td>
            </tr>
{#            <tr>#}
{#                <th>PrixUnitaire</th>#}
{#                <td>{{ buy_carburant.prixUnitaire }}</td>#}
{#            </tr>#}
            <tr>
                <th>Prix</th>
                <td>{{ buy_carburant.prix }}</td>
            </tr>
            <tr>
                <th>Date d'achat</th>
                <td>{{ buy_carburant.buyAt ? buy_carburant.buyAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date d'enregistrement</th>
                <td>{{ buy_carburant.createdAt ? buy_carburant.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_buy_carburant_index') }}">Retour a la liste</a>

    <a href="{{ path('app_buy_carburant_edit', {'id': buy_carburant.id}) }}">Mise a jour</a>

    {{ include('station-dashboard/buy_carburant/_delete_form.html.twig') }}
{% endblock %}
