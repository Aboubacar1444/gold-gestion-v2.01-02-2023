<form method="post" action="{{ path('app_buy_carburant_delete', {'id': buy_carburant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ buy_carburant.id) }}">
    <button class="btn">Supprimer</button>
</form>
